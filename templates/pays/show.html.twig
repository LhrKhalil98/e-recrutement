{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Pays</th>
                <td>{{ pay.getIdPays()}}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('pays_edit', {'id_pays': pay.getIdPays()}) }}">Modifier</a>

    {{ include('pays/_delete_form.html.twig') }}
{% endblock %}
