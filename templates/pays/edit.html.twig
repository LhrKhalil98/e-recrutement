{% extends 'base.html.twig' %}

{% block title %}Modifier Pays{% endblock %}

{% block body %}
      <div class="col-sm-12">
                    <nav aria-label="breadcrumb">
   <ol class="breadcrumb">
      <li class="breadcrumb-item" ><a  href="{{ path('pays_index' ) }}" >Pays</a></li>
    
      <li class="breadcrumb-item active" aria-current="page">Modifier {{pay.getIdPays() }}   </li>
   </ol>
</nav>
        <div class="card">
                     <div class="card-header d-flex justify-content-between">
                        <div class="header-title">
                           <h4 class="card-title">Modifier Pays </h4>
                        </div>
                    </div>
                    <div class="card-body">
    {{ include('pays/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('pays/_delete_form.html.twig') }}
             </div>

                    </div>

                    </div>
{% endblock %}
