<form method="post" action="{{ path('entretien_delete', {'id': entretien.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entretien.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
