<form method="post" action="{{ path('categorie_offre_delete', {'id': categorie_offre.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_offre.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
