{{ form_start(form) }}
      <div class="form-group"> 
    {{ form_row(form.cat_description) }}
    </div>
      <div class="form-group"> 
    {{ form_row(form.abreviation) }}
    </div>
      <div class="form-group"> 
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
