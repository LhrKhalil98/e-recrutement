{% extends 'base.html.twig' %}

{% block title %}Modifier Utilisateur{% endblock %}

{% block body %}
            <div class="col-sm-12">
                 <nav aria-label="breadcrumb">
   <ol class="breadcrumb">
      <li class="breadcrumb-item" ><a  href="{{ path('user_index' ) }}" >Utulisateurs</a></li>
    
      <li class="breadcrumb-item active" aria-current="page">Modifier utulisateurs  </li>
   </ol>
</nav>
        <div class="card">
                     <div class="card-header d-flex justify-content-between">
                        <div class="header-title">
                           <h4 class="card-title">Modifier utulisateurs </h4>
                        </div>
                    </div>
                    <div class="card-body">


    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('user/_delete_form.html.twig') }}
      </div>
    </div>
    </div>
{% endblock %}
