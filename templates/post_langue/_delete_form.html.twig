<form method="post" action="{{ path('post_langue_delete', {'id': post_langue.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_langue.id) }}">
    <button   class=" btn btn-danger">Supprimer</button>
</form>
