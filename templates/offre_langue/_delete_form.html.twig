<form method="post" action="{{ path('offre_langue_delete', {'id': offre_langue.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_langue.id) }}">
    <button class="btn">Suprimer</button>
</form>
