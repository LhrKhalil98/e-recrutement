{% extends 'base.html.twig' %}

{% block title %}OffreLangue{% endblock %}

{% block body %}
    <h1>OffreLangue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre_langue.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre_langue.description }}</td>
            </tr>
            <tr>
                <th>Mission</th>
                <td>{{ offre_langue.mission }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offre_langue_index') }}">back to list</a>

    <a href="{{ path('offre_langue_edit', {'id': offre_langue.id}) }}">edit</a>

    {{ include('offre_langue/_delete_form.html.twig') }}
{% endblock %}
